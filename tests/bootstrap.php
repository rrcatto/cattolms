<?php

/*
Generated by: Claude Code CLI

Date time: 2026/08/21 15:04 SAST

Version: 0.5.7.6

Description:
PHPUnit bootstrap. Registers the Composer autoloader and performs the one process-wide side
effect the test suites share: constructing Fat-Free's Base.

Base's constructor installs a global error handler and a global exception handler. That is a
one-time, process-wide effect, so it belongs here rather than in whichever test happens to
construct F3 first. Left to the tests, PHPUnit charges the first one to touch F3 as risky for
"did not remove its own error handlers" and then strips those handlers for the rest of the run.
That is not cosmetic: PaginationRenderSmokeTest depends on F3's error handling to detect a
template reading an undefined variable, and once the handlers were stripped it silently stopped
detecting anything from its second data set onward.

Architectural boundary: bootstrap only. This file must not load application configuration, open a
database connection or build the container. Integration tests that need those bootstrap through
CliBootstrap themselves, which resolves the instance .env.

Changelog:
2026/08/21 15:04 SAST
- Created so F3's global handler installation happens once, before any test runs, instead of being attributed to and then undone by the first test that constructs Base.
*/

declare(strict_types=1);

// Guarded so the suite can also run under a configuration that has already registered an
// autoloader, for example a workarea checkout whose vendor directory sits outside the code root.
$autoloader = dirname(__DIR__) . '/vendor/autoload.php';
if (is_file($autoloader)) {
    require_once $autoloader;
}

Base::instance();
