<?php

/*
Generated by: Claude Code CLI

Date time: 2026/09/10 01:40 SAST

Version: 0.7

Description:
The test bootstrap.

It used to exist for one reason: constructing Fat-Free's Base once, up front. Base's constructor
installed a global error handler and a global exception handler, so whichever test happened to
construct it first was blamed for the side effect and the next test undid it.

Fat-Free is gone, so that reason is gone with it. What remains is the autoloader, which is all a
test needs.

Changelog:
2026/09/10 01:40 SAST
- Reduced to the autoloader when Fat-Free was removed.
2026/09/06 SAST
- Created so F3's global handler installation happened once rather than inside the first test.
*/

declare(strict_types=1);

require __DIR__ . '/../vendor/autoload.php';
