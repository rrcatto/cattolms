# Catto Learning 0.5.4 — Theme Studio colour-system hotfix

Generated by: ChatGPT
Date time: 2026/08/12 04:03 SAST
Version: 0.5.4 (no version bump)

This cumulative hotfix can be applied directly to the released 0.5.4 tree whether or not the earlier theme-runtime hotfix was already applied.

It fixes:
- package themes inheriting essential Catto Learning core CSS/JS;
- palette-specific semantic role mappings;
- rational automatic role assignment for each five-colour palette;
- live iframe preview while colours/roles are edited;
- full-width readable hex fields, with Custom hex explicitly editable;
- Aurum/Gilt and other package themes responding to semantic variables;
- draft/live snapshot stamping so Save draft does not silently become live.

After unzipping into `/usr/local/lib/php/catto-learning/current`, publish the public assets:

```bash
rsync -a /usr/local/lib/php/catto-learning/current/public_html/ /home/prettythings/public_html/
```

No database migration or reset is required.
