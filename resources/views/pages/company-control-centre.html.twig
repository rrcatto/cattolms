{% extends layout %}
{% block page_body %}
	{% set ph_kicker = 'Company' %}
	{% set ph_title = 'Company workspace' %}
	{% set ph_lead = 'Every company section in one place.' %}
	{% include '@platform/partials/page-head.html.twig' %}
	{# Every workspace opens on a card, as its sections do. #}
	{% embed ui_template('layout.section-grid') with {props: ui_props('layout.section-grid', {columns: 'auto', section_spacing: true})} %}
{% block body %}

		{% embed ui_template('layout.surface') with {props: ui_props('layout.surface', {})} %}
{% block surface_body %}

			{{ ui('layout.section-head', {heading: 'Your company workspace', eyebrow: 'All sections', summary: 'Every company section in one place. Each one also has a page of its own.'}) }}

{% endblock %}
{% endembed %}

{% endblock %}
{% endembed %}
<section class="cl-company-workspace" data-company-workspace>
		{% include '@platform/partials/company-context.html.twig' %}
		{% for section in company_sections %}
{#
			The section fetches its own table the first time it is opened. The request attributes
			sit on the details element itself, which is the element that fires the toggle event: an
			earlier version put them on the inner placeholder and listened through a from selector,
			and the request never fired at all, so every section sat on Loading forever.

			The attributes are literal markup with only the route interpolated. Built as a string -
			'hx-get="' ~ route ~ '"' - Twig autoescapes the quotes and the browser receives
			hx-get=&quot;...&quot; as text, so the request never fired for a second reason on top
			of the first. Never assemble an attribute as an expression.
		#}
			{% embed ui_template('overlay.accordion-section') with {props: ui_props('overlay.accordion-section', {heading: (section.label), summary: (section.description), open: section.key == 'dashboard', workspace: 'company', section_key: (section.key), load_url: section.key != 'dashboard' ? section.route : ''})} %}
{% block body %}

					{% if section.key=='dashboard' %}
						{{ section.content|raw }}
					{% else %}
						<div class="cl-ui-accordion-lazy">
							<p class="help cl-ui-accordion-loading">
								Loading {{ section.label }}…
							</p>
							<noscript>
								<p class="help">
									Open <a href="{{ section.route }}">{{ section.label }}</a> to see this section.
								</p>
							</noscript>
						</div>
					{% endif %}

{% endblock %}
{% block actions %}

						<a class="cl-ui-accordion-link" href="{{ section.route }}">Open as standalone page</a>

{% endblock %}
{% endembed %}
		{% endfor %}
	</section>
{% endblock %}
