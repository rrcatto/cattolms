{#
Generated by: Claude Code CLI
Date time: 2026/09/07 15:10 SAST
Version: 0.6
Description:
  One Administration section on its own route.

  The heading is the shared page header every screen on the platform uses. The label and description
  are resolved by the server from AdministrationSectionRegistry, so this page never names a section
  itself and never decides which sections a reader may see.

  Comments here are plain prose only: no angle brackets, no comment delimiters, no template tokens
  and no code. Each of those is read as markup rather than as text.

Changelog:
  2026/09/07 15:10 SAST
  - Uses the shared page header rather than a heading block of its own.
#}
{% extends layout %}

{% block page_body %}
{% set ph_kicker = 'Administration' %}
{% set ph_title = admin_section.label %}
{% set ph_lead = admin_section.description %}
{% set ph_action_href = can_access_admin ? '/admin' : '' %}
{% set ph_action_label = 'All administration' %}
{% include '@platform/partials/page-head.html.twig' %}

<section class="section cl-admin-standalone-section" data-admin-section="{{ admin_section.key }}">
  {{ admin_section.content|raw }}
</section>
{% endblock %}
