{#
Generated by: Claude Code CLI
Date time: 2026/09/07 17:20 SAST
Version: 0.6
Description:
  The tab strip shared by the two halves of Companies: Course Consumers and Course Creators.

  Companies were one list with a Type column, and the two kinds of company are asked different
  questions. A consumer is described by its staff and their enrolments; a creator by the courses it
  owns. One table carrying both sets of figures left most of every row empty and made the reader
  filter by type in their head before the list meant anything.

  A company that both sells and buys appears on both tabs, because that is what it is. The split is
  on what a company does, not on a field that would force it to be one thing.

  Rendered in the same place on both screens, immediately above the section body, for the same
  reason the Courses group strip is: it is what makes two routes read as one interface.

  WARNING: a template comment is not a safe place to document syntax. A comment delimiter
  written inside a comment body ends the comment early and leaks the remainder onto the page.
  Keep every comment plain prose: no comment delimiters and no code, not even to document one.
Changelog:
  2026/09/07 17:20 SAST
  - Created with the Companies split.
#}
<nav class="cl-group-nav" aria-label="Company administration">
	<a class="cl-group-tab {{ company_group == 'consumers' ? 'is-current' : '' }}" href="/admin/companies" {% if company_group == 'consumers' %}aria-current="page"{% endif %}>Course Consumers</a>
	<a class="cl-group-tab {{ company_group == 'creators' ? 'is-current' : '' }}" href="/admin/companies/creators" {% if company_group == 'creators' %}aria-current="page"{% endif %}>Course Creators</a>
</nav>
