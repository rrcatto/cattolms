{% embed ui_template('layout.section-grid') with {props: ui_props('layout.section-grid', {columns: 'auto', section_spacing: true})} %}
{% block body %}

	{% embed ui_template('layout.surface') with {props: ui_props('layout.surface', {})} %}
{% block surface_body %}

		{{ ui('layout.section-head', {heading: 'Companies on the platform', eyebrow: 'Companies', summary: 'Companies whose staff are trained, and the System Company.'}) }}
		{% include '@platform/partials/admin/company-group-nav.html.twig' %}
		{% embed ui_template('feedback.notice') with {props: ui_props('feedback.notice', {tone: 'info', section_spacing: false})} %}
{% block body %}

			<div>
				<strong>System Company:</strong> the permanent host organisation. It cannot be disabled or deleted.
			</div>

{% endblock %}
{% endembed %}

		{% embed ui_template('layout.toolbar') with {props: ui_props('layout.toolbar', {})} %}
{% block toolbar_primary %}

{% endblock %}
{% block toolbar_actions %}

			{% if can_manage_companies %}
				{{ ui('action.button', {label: '＋ Add Company', variant: 'primary', size: 'normal', type: 'button', modal: 'company-add', icon: 'action-add'}) }}
			{% endif %}

{% endblock %}
{% endembed %}
		{% include '@platform/partials/admin/company-modals.html.twig' with {cm_rows: companies} %}
		{% embed ui_template('data.dataset') with {props: ui_props('data.dataset', {name: 'companies', pagination: companies_pagination, search: {ds_name: 'companies', ds_label: 'Search course consumers', ds_value: companies_search, ds_action: '/admin/companies'}})} %}
{% block results %}

					{% embed ui_template('data.table') with {props: ui_props('data.table', {})} %}
{% block header %}

							<tr>
								{% for sh in companies_sort_headers %}
									{% include '@platform/partials/sortable-header.html.twig' with {sh: sh, sort_region: companies_region, sort_results: companies_results} %}
								{% endfor %}
							</tr>

{% endblock %}
{% block body %}

							{% for company in companies %}
								<tr>
									<td title="{{ company.name }} · {{ company.domain }}">
										<strong>{{ company.name }}</strong>
										{% if company.is_system %}
											{{ ui('feedback.badge', {label: 'System', tone: 'permanent'}) }}
										{% endif %}
									</td>
									<td>
										{{ company.people_count }}
									</td>
									<td>
										{{ company.enrolment_count }}
									</td>
									<td>
										{{ ui('feedback.badge', {label: (company.status_label), tone: company.status=='active' ? 'success' : 'danger'}) }}
									</td>
									<td class="cell-actions">
										{% embed ui_template('action.row') with {props: ui_props('action.row', {menu: true})} %}
{% block body %}

												{% if can_select_company and company.status=='active' %}
													<form action="/company/context" method="post">
														<input type="hidden" name="csrf" value="{{ csrf }}">
														<input type="hidden" name="company_id" value="{{ company.id }}">
														{{ ui('action.button', {label: 'Manage', variant: 'primary', size: 'small', type: 'submit'}) }}
													</form>
												{% endif %}
												{% if can_manage_companies %}
													{{ ui('action.button', {label: 'Edit', variant: 'secondary', size: 'small', type: 'button', modal: 'company-edit-' ~ (company.id)}) }}
												{% endif %}

{% endblock %}
{% endembed %}
									</td>
								</tr>
							{% endfor %}

{% endblock %}
{% endembed %}

{% endblock %}
{% block preview %}

					{% if companies_preview_has_more %}
						<div class="pagination-row">
							<span class="pagination-summary">Showing the first {{ preview_page_size }} companies.</span>
							{{ ui('action.link', {label: 'View all course consumers', variant: 'secondary', size: 'small', href: '/admin/companies'}) }}
						</div>
					{% endif %}

{% endblock %}
{% endembed %}

{% endblock %}
{% endembed %}

{% endblock %}
{% endembed %}
