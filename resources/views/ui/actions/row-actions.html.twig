{% if props.menu %}<details class="cl-ui-row-menu" data-cl-dropdown><summary class="cl-ui-row-trigger" aria-label="Actions">{{ ui('icon', {name: 'action-open'}) }}<span class="cl-ui-row-label">Actions</span> <span aria-hidden="true">▾</span></summary><div class="cl-ui-row-panel">{% block body %}{% endblock %}</div></details>{% else %}<div class="cl-ui-row-actions">{{ block('body') }}</div>{% endif %}
