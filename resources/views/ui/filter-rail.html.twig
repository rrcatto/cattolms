{% if items %}
	<nav class="cl-filter-rail" aria-label="{{ label }}">
		<span class="cl-filter-rail-label">{{ label }}</span>
		{% for item in items %}{{ ui('filter-pill', {item: item}) }}{% endfor %}
	</nav>
{% endif %}
