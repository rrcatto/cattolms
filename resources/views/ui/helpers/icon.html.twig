<svg class="cl-ui-icon cl-ui-icon--{{ props.size }}"{% if props.decorative %} aria-hidden="true"{% else %} role="img" aria-label="{{ props.label }}"{% endif %} focusable="false"><use href="/img/nav-icons.svg#{{ props.name }}"></use></svg>
