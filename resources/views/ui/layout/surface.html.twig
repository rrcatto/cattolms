<section class="cl-ui-surface cl-ui-surface--{{ props.variant }}{% if props.section_spacing %} cl-ui-spaced{% endif %}{% if props.sticky %} cl-ui-sticky{% endif %}" data-tone="{{ props.tone }}"{% if props.id %} id="{{ props.id }}"{% endif %}>
{% block surface_body %}{% endblock %}
</section>
