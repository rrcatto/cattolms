<?php

/*
Generated by: Claude Code CLI

Date time: 2026/09/08 22:15 SAST

Version: 0.7

Description:
The authenticated caller's own profile document.

Architectural boundary: HTTP controller. It states what access the route requires and returns what
the identity already knows about itself; it holds no business rules and no SQL.

The two attributes are the platform's two orthogonal access questions written out. The scope says
what the token was issued for and the permission says what the account may do, and both must pass -
an API call is never allowed to do something the equivalent web operation would refuse, and a token
is never allowed to do something outside what it was issued for.

Changelog:
2026/09/08 22:15 SAST
- Ported from Http\Controller\Api\ApiProfileController.
*/

declare(strict_types=1);

namespace CattoLearning\Http\Symfony;

use CattoLearning\Api\ApiScope;
use CattoLearning\Api\Security\ApiUser;
use Symfony\Component\HttpFoundation\JsonResponse;
use Symfony\Component\Routing\Attribute\Route;
use Symfony\Component\Security\Http\Attribute\CurrentUser;
use Symfony\Component\Security\Http\Attribute\IsGranted;

final class ApiProfileController
{
    #[Route('/api/v1/me', name: 'api_profile_me', methods: ['GET'])]
    #[IsGranted(ApiScope::PROFILE_READ)]
    #[IsGranted('ACCOUNT.PROFILE.VIEW')]
    public function me(#[CurrentUser] ApiUser $user): JsonResponse
    {
        return ApiResponse::data($user->identity->toArray());
    }
}
