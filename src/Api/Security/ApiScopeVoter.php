<?php

/*
Generated by: Claude Code CLI

Date time: 2026/09/08 22:15 SAST

Version: 0.7

Description:
Decides whether an API token carries a transport scope.

Architectural boundary: authorization. It answers one of the three orthogonal access questions and
must never answer another: a scope says what the token was issued for, not what the person behind
it may do. The business permission is a separate check, made by PermissionVoter, and an API route
states both.

The two vocabularies cannot collide by construction: scopes are lowercase and colon-separated
(courses:read), permissions are uppercase dot notation (COURSE.EDIT), so each voter recognises only
its own attributes and abstains on everything else.

Changelog:
2026/09/08 22:15 SAST
- Created with the API firewall.
*/

declare(strict_types=1);

namespace CattoLearning\Api\Security;

use CattoLearning\Api\ApiScope;
use Symfony\Component\Security\Core\Authentication\Token\TokenInterface;
use Symfony\Component\Security\Core\Authorization\Voter\Voter;

/** @extends Voter<string,mixed> */
final class ApiScopeVoter extends Voter
{
    protected function supports(string $attribute, mixed $subject): bool
    {
        return ApiScope::isValid($attribute);
    }

    protected function voteOnAttribute(string $attribute, mixed $subject, TokenInterface $token): bool
    {
        $user = $token->getUser();

        return $user instanceof ApiUser && $user->identity->hasScope($attribute);
    }
}
