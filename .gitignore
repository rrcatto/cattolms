/vendor/
/.env
/.env.local
/.env.*.local
/public_html/themes/*
!/public_html/themes/.gitkeep
# composer analyse writes its PHPStan tmpDir here (phpstan.neon); build cache never ships.
/storage/
/.phpunit.cache/
/.phpstan.cache/
/coverage/
/.idea/
/.vscode/
.DS_Store
Thumbs.db
*.log
/generated-themes/
*.rej
*.orig
*.orig.*

# Symfony writes this while building the container, and its contents depend on which environment
# was warmed last, so a tracked copy churns between commits. It is an IDE helper; nothing reads it.
/config/reference.php
